# mayak-digit: Цифровой словарь Маяковского

![Python](https://img.shields.io/badge/python-3.8+-blue.svg)
![Status](https://img.shields.io/badge/status-active-success.svg)

**MayakDigit (Mayak-2D)** — это цифровой проект по созданию полноценного кроссжанрового авторского словаря В. В. Маяковского, совмещающий в себе инстурменты цифровой филологии и NLP.

В данный момент проект существует в статусе прототипа.

---

## 🚀 Основные функции

* **Индекс Маяка (Proximity Index):** Глобальный расчет весов близости слов по всему корпусу.
* **Семантический компаратив (Navec):** Сравнение авторского словоупотребления с общеязыковой нормой. Система находит синонимы в модели Navec и разделяет их на:
    * *Используемые автором* (с расчетом их личного контекстного веса).
    * *Проигнорированные автором* (семантические лакуны).
* **Умная лемматизация и фильтрация:** Предобработка текстов с использованием морфологического анализа для отсечения дублей и нормализации выдачи синонимов.
* **Сравнение периодов (Period Comparison):** Анализ семантического развития авторского словоупотребления — сравнение индексов контекстной близости слова в разные периоды творчества (дореволюционный vs 1920-е и т.д.) с визуализацией дельта-метрик.
* **MiniLM:** Дообучение Sentence Transformers на корпусе Маяковского для специализации модели на поэтическом стиле и разовом анализе семантических сдвигов между периодами. (В данный момент находится в разработке)

---

## 🛠 Технологический стек

* **Язык:** Python 3.9+
* **Интерфейс:** Streamlit
* **NLP:** PyMyStem (лемматизация), Navec (эмбеддинги), Sentence Transformers (TSDAE fine-tuning), Ollama/Anthropic (LLM-интерпретация)
* **Data:** Pandas, Parquet (оптимизированное хранение)
* **Логика:** Глобальный итеративный поиск по границам предложений и строк.

---

## Источники

Для создания поэтического корпуса использованы предварительно обработанные данные из цифрового корпуса PoeTree (https://versologie.cz/poetree/browser/author?corpus=ru&id_author=18)

Cсылка на скачивание полного корпуса стихотворений: https://drive.google.com/drive/folders/1zskVJAW-01XhPkRfT9vcxAoorS3gIlcT?usp=sharing

Для создания публицистического корпуса использованы материалы Полного собрания сочинений в 13 томах (М.: Гослитиздат, 1955-1959.) (публицистический корпус находится в разработке)

Для сопоставления метрик использован предварительно обработанный дамп поэтических текстов из корпуса PoeTree.

## 📂 Структура проекта

Структура проекта в данный момент находится в разработке

```text
mayak-digit/
├── app/
│   └── app.py              # Веб-интерфейс на Streamlit
├── src/
│   ├── analyzer.py         # Ядро: расчет связей и работа с Navec
│   ├── file_utils.py       # Вспомогательные функции для работы с файлами и путями
│   └── text_utils.py       # Морфология и лемматизация
├── models/
│   ├── navec_hudlit_...tar # Векторная модель Navec
├── data/
│   ├── author_vocabulary.txt # Список всех уникальных слов в текстах автора
│   ├── database_example.csv  # CSV-пример структуры базы данных
│   ├── database.parquet      # Оптимизированная БД произведений (634 стихотворения)
│   ├── lemmatization_mismatches.json # фиксирует разночтения лемматизаторов pymorphy3 и pymystem
│   ├── mayak_hapax.json # уникальные леммы Маяка по сравнению с корпусом PoeTree
│   └── vocabulary_forms.json # Леммы и словоформы Маяка (результат лемматизации через PyMyStem)
├── src/
│   ├── analyzer.py             # Ядро: расчет связей и работа с Navec
│   ├── file_utils.py           # Вспомогательные функции
│   ├── preprocessor.py         # Миграция и обработка Parquet
│   └── text_utils.py           # Морфология и лемматизация
└── README.md
```

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/yourusername/mayak-digit.git](https://github.com/yourusername/mayak-digit.git)
   cd mayak-digit

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt

3. **Подготовьте модели:**
   Скачайте предобученную модель Navec (рекомендуется hudlit) и поместите файл .tar в директорию models/

4. **Запустите приложение:**
    ```bash
    streamlit run app/app.py

## Методология: Как считается "Вес"?

Для каждого вхождения искомого слова (таргета) система сканирует всё произведение целиком. В отличие от стандартных методов окон (window size), наш алгоритм учитывает глобальный контекст. 

Итоговый вес слова-соседа вычисляется по формуле затухания:

$$Weight = (decay_{dist}^{d}) \times (decay_{brk}^{n\_brks}) \times (decay_{sent}^{n\_sents})$$

Где:
* **$d$** — физическое расстояние в словах (чем дальше, тем меньше связь).
* **$n\_brks$** — количество переносов строк («лесенка»). Каждый разрыв уменьшает семантическую близость.
* **$n\_sents$** — количество границ предложений. Переход в другое предложение считается сильным барьером.

Модель автоматически суммирует веса от всех вхождений слова в тексте, что позволяет адекватно оценивать рефрены и кольцевые композиции, характерные для поэтики Маяковского.

## 📝 Dashboard

Проект находится в стадии активной разработки. Ниже отмечены уже реализованные обновления и ближайшие планы:

- [x] **LLM-интерпретатор:** Интеграция нейросети для автоматического филологического разбора разницы в окружении синонимов (уже интегрирована интерпертация с Ollama и DeepSeek (по API), в ближайших обновлениях - интерпретация через Antrophic)
- [x] **Хронологический срез:** Возможность сравнивать семантику одного и того же слова в разные периоды творчества (например, дореволюционный период vs 1920-е).
- [x] **Визуализация графов:** Построение семантических сетей через для наглядного отображения "спутников" слова (уже реализовано для общей карты частотных слов, в дальнейшем планируется реализация для каждого отдельного слова).
- [ ] **Экспорт отчетов:** Генерация PDF-карточек слова для использования в лингвистических исследованиях.
- [ ] **MiniLM** Интеграция модели MiniLm (Sentence-Transformers) и её дообучение на корпусе Маяковского
---
*Проект разработан в рамках исследования цифровых методов анализа поэтических текстов В. В. Маяковского (НИУ ВШЭ, Нижний Новгород).*